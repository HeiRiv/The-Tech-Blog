<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/jass.css">
</head>

<div class="row">
  <div class="welcome col-auto">
    <h2>Welcome {{name}}, this is an space made specially for you to blog about Tech!</h2>
  </div>
</div>

    <form class="form new-fact-form">
      <div class="form-group">
        <label for="fact-title">Fact Title:</label>
        <input class="form-input" type="text" id="fact-title" name="fact-title" />
      </div>

      <div class="form-group">
        <label for="fact-body">Start here...</label>
        <textarea class="form-input" id="fact-body" name="fact-body"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary save-fact">Save</button>
      </div>
    </form>
  </div>

  {{#if fact.length}}
  <div class="fact row">
    <h3>Your facts:</h3>

    {{#each fact as |fact|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/fact/{{fact.id}}">{{fact.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{fact.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
{{/if}}

<script src="/js/profile.js"></script>

